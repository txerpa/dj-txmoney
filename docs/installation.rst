============
Installation
============

At the command line::

    $ easy_install dj-txmoney

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-txmoney
    $ pip install dj-txmoney
